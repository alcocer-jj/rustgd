% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frames.R
\name{.rustgd_frames_ensure}
\alias{.rustgd_frames_ensure}
\title{Internal: launch the frames binary for this directory if it is not already
the one we have running. Liveness is decided up front in \code{view()} via
\code{\link[=.rustgd_frames_alive]{.rustgd_frames_alive()}}, which clears \code{.rustgd_state$frames_dir} when the
window is gone, so here we simply launch whenever it is not our active
directory.}
\usage{
.rustgd_frames_ensure(frames)
}
\description{
Internal: launch the frames binary for this directory if it is not already
the one we have running. Liveness is decided up front in \code{view()} via
\code{\link[=.rustgd_frames_alive]{.rustgd_frames_alive()}}, which clears \code{.rustgd_state$frames_dir} when the
window is gone, so here we simply launch whenever it is not our active
directory.
}
