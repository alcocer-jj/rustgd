% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/webview.R
\name{rustgd_view}
\alias{rustgd_view}
\title{Internal: the function registered as \code{options(viewer)}. Receives the path
to a rendered HTML file (or a URL) from htmlwidgets. For a file, it copies
the widget's bundle (its \code{index.html} and \verb{lib/}) into the widget directory
as \verb{widget-NNNN/}, writes a \code{widget-NNNN.txt} descriptor next to it, and
makes sure a webview window is running. A URL is handed to
\code{\link[=.rustgd_push_url]{.rustgd_push_url()}} and shown in place.}
\usage{
rustgd_view(url, height = NULL, ...)
}
\description{
Internal: the function registered as \code{options(viewer)}. Receives the path
to a rendered HTML file (or a URL) from htmlwidgets. For a file, it copies
the widget's bundle (its \code{index.html} and \verb{lib/}) into the widget directory
as \verb{widget-NNNN/}, writes a \code{widget-NNNN.txt} descriptor next to it, and
makes sure a webview window is running. A URL is handed to
\code{\link[=.rustgd_push_url]{.rustgd_push_url()}} and shown in place.
}
