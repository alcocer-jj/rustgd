% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/webview.R
\name{rustgd_shiny_launch}
\alias{rustgd_shiny_launch}
\title{Internal: the function registered as \code{options(shiny.launch.browser)}. Shiny
calls this with the running app's URL once the server is listening. We show
it in the rustgd window and register a \code{\link[shiny:onStop]{shiny::onStop()}} callback that
removes this app's entry when the app exits. Because \code{onStop} fires when
\code{runApp()} exits (including when you interrupt it with Ctrl+C), stopping the
app both frees the terminal and clears the now dead pane from the window, so
there is no separate clear step. If showing it fails, fall back to the
default browser, and never let an error here interrupt \code{runApp()}.}
\usage{
rustgd_shiny_launch(url, ...)
}
\description{
Internal: the function registered as \code{options(shiny.launch.browser)}. Shiny
calls this with the running app's URL once the server is listening. We show
it in the rustgd window and register a \code{\link[shiny:onStop]{shiny::onStop()}} callback that
removes this app's entry when the app exits. Because \code{onStop} fires when
\code{runApp()} exits (including when you interrupt it with Ctrl+C), stopping the
app both frees the terminal and clears the now dead pane from the window, so
there is no separate clear step. If showing it fails, fall back to the
default browser, and never let an error here interrupt \code{runApp()}.
}
